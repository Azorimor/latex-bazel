load("@bazel_latex//:latex.bzl", "latex_document")

latex_document(
    name = "main",
    main = "main.tex",
    srcs = glob(["sections/*.tex"]) + 
           glob(["images/*"], allow_empty = True) + [
        "main.tex",
        "@bazel_latex//packages:graphicx",
    ],
)
